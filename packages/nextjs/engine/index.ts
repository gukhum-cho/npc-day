/** Game engine. Handles the main game loop. */
const Engine: {
  load: (saveString: string) => void;
  start: () => void;
} = {
  load: function (saveString) {
    console.log("LOai")
  },

  start: function () {
  },
}

export { Engine };
